{
  "name": "hartsystem-fullstack",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w @hartsystem/api",
    "db:seed": "npm run db:seed -w @hartsystem/api",
    "dev": "npm run dev -w @hartsystem/api & npm run dev -w @hartsystem/web",
    "dev:stack": "npm run db:up && npm run db:migrate && npm run db:seed && npm run dev",
    "build": "npm run build -w @hartsystem/api && npm run build -w @hartsystem/web",
    "lint": "npm run lint -w @hartsystem/api && npm run lint -w @hartsystem/web"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}
